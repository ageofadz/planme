import Button from '@mui/material/Button'
import React from 'react'

const LoginButton = (): React.JSX.Element => {
  return <Button color="inherit" href='/api/auth/login'>Log In</Button>
}

export default LoginButton
